const circleContainerHeight = 500;
const circleHeightWidth = 60;
const totalAmountOfCircle = 5;
const circleVerticalMargin =
  (circleContainerHeight - totalAmountOfCircle * circleHeightWidth) /
  ((totalAmountOfCircle + 1) * 2);
const borderRadius = 36;
const waterBackgroundColor = "#00224B";

export {
  circleContainerHeight,
  circleHeightWidth,
  totalAmountOfCircle,
  circleVerticalMargin,
  borderRadius,
  waterBackgroundColor
};
